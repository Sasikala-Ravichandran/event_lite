<h1>Show page of Events</h1>
<p><%= @event.name %></p>
<p><%= @event.venue %></p>
<% if current_user == @event.creator %>
  <%= link_to "Edit", edit_event_path(@event) %>
  <%= link_to "Delete", event_path(@event), method: :delete %>
<% end %>